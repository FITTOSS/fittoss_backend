// HOME
const HOME = "/api";

// local register
const REGISTER = "/register";

// local login
const LOGIN = "/login";

// email confirm
const EMAIL_CONFIRM = "/email";

export const routes = {
  home: HOME,
  register: REGISTER,
  login: LOGIN,
  emailConfirm: EMAIL_CONFIRM,
};
